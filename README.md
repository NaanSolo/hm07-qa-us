# Sprint 7 project by Muhammad Ali
compiled HTTPS test cases to run on VS studio. 
Used GET, POST, PUT and DELETE methods.
Run 'npx jest' in terminal to run all test cases.
Restart server, if deleteHandlers.test.js case needs to be executed more than once.